import React from "react";
import { TxtContainerStyled } from "./AboutStyles";

const TxtContainer = ({ children }) => {
	return <TxtContainerStyled>{children}</TxtContainerStyled>;
};

export default TxtContainer;
